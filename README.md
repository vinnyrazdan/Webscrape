# Webscrape

Small command-line utility for scraping basic data from an HTML page.

## Usage

```bash
python scraper.py https://example.com
```

The command prints JSON containing:

- `url`
- `title`
- `links`
- `text`